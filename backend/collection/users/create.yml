info:
  name: create
  type: http
  seq: 2

http:
  method: POST
  url: "{{URL}}"
  body:
    type: json
    data: |-
      {
        "name": "Bob", 
        "email": "bob@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
